{
  "name": "@m3m3n70/holon",
  "version": "v0.0.1-alpha",
  "description": "An interface to the Holon Universe",
  "main": "dist/index.js",
  "scripts": {
    "test": "npm run test:unit && npm run test:integration",
    "pretest": "npm run lint",
    "test:unit": "jest -c jest.config.js",
    "test:unit:watch": "jest -c jest.config.js --watch",
    "test:integration": "jest -c jest.integration.config.js",
    "build": "rimraf dist && tsc",
    "lint": "tslint --project tsconfig.json"
  },
  "author": "neo.wilder",
  "license": "ISC",
  "repository": "git://github.com/m3m3n70/holon.git",
  "dependencies": {
    "rimraf": "^3.0.0",
    "web3": "^1.2.1"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "jest": "^24.9.0",
    "ts-jest": "^24.1.0",
    "tslint": "^5.20.0",
    "typescript": "^3.6.3"
  },
  "files": [
    "dist/**"
  ]
}
